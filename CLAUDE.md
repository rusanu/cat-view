@AGENTS.md

# Development Server

**IMPORTANT**: The development server (`npm start`) is already running as a VS Code task. Do NOT attempt to start or kill the server. The app is available at http://localhost:4200.
